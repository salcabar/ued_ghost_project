<div class="nav">
    <h3 class="icon nav-title">Menu</h3>
    <a href="#" class="nav-close">
        <span class="hidden">关闭</span>
    </a>
    <ul>
        {{#foreach navigation}}
            <li class="{{#if current}} nav-current{{/if}}" role="presentation"><a href="{{url absolute="true"}}" class="icon nav-{{slug}}">{{label}}</a></li>
        {{/foreach}}
    </ul>
</div>